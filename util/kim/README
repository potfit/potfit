###############################################################################
#
# Contributor: Mingjian Wen (wenxx151@umn.edu), University of Minnesota 
#
###############################################################################

The file `create_EAM_setfl_model.c' is used to create a KIM EAM model in Dynamo
format with an arbitrary number of data points.  The user can choose from multiple 
KIM EAM Model Drivers to write this Model.  Note that it just create a template
and the actual values of the `phi', `embedding', and `density' data are not given 
(0.0 is used to filled the places).  The user is responsible to fill the data.  

Run this code by:

% gcc create_EAM_setfl_model.c -o exec_create_model
% ./exec_create_model
