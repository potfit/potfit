The following files are changed: 

MAKEFILE

potfit.h  # actually, they are the same, only a few comments are different
potfit.c # DONE

config.c # DONE 

potential_input.c  # DONE

simann.c # DONE
powell_lsq.c  #DONE    only changed write_pot_file
diff_evo.c  #DONE    only changed write_pot_file

types.h # DONE

utils.h # only inline functions are here, (ideally, no modification needed ) keyword "modified" is used 
utils.c
